# Generated from Workflow.pkl. DO NOT EDIT.
name: Release
'on':
  push:
    branches-ignore:
    - '**'
    tags:
    - '**'
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false
permissions:
  contents: read
jobs:
  build-release:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6
      with:
        persist-credentials: false
        fetch-depth: 0
    - uses: actions/setup-java@1bcf9fb12cf4aa7d266a90ae39939e61372fe520 # v5
      with:
        java-version: '25'
        distribution: temurin
    - if: github.repository != 'apple/pkl-lsp'
      run: |-
        git remote add upstream https://github.com/apple/pkl-lsp.git
        git fetch upstream main
        echo 'PKL_ORIGINAL_REMOTE_NAME=upstream' > "$GITHUB_ENV"
    - name: Set DEBUG_ARGS env var
      env:
        RUNNER_DEBUG: ${{ runner.debug }}
      shell: bash
      run: |-
        if [[ "$RUNNER_DEBUG" -eq 1 ]]; then
          echo "DEBUG_ARGS=--info --stacktrace" >> $GITHUB_ENV
        fi
    - run: ./gradlew $DEBUG_ARGS -DreleaseBuild=true check shadowJar verifyDistribution
    - name: Upload build artifacts
      if: '!cancelled()'
      uses: actions/upload-artifact@330a01c490aca151604b8cf639adc76d48f6c5d4 # v5
      with:
        name: pkl-lsp-build-artifacts
        path: build*/libs/
    - name: Upload Test Result XML
      if: '!cancelled()'
      uses: actions/upload-artifact@330a01c490aca151604b8cf639adc76d48f6c5d4 # v5
      with:
        name: test-results-xml-build-release
        path: build/test-results/**/*.xml
  test-macos:
    if: github.repository_owner == 'apple'
    needs:
    - build-release
    runs-on:
    - self-hosted
    - macos
    steps:
    - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6
      with:
        persist-credentials: false
    - uses: actions/setup-java@1bcf9fb12cf4aa7d266a90ae39939e61372fe520 # v5
      with:
        java-version: '25'
        distribution: temurin
    - uses: actions/download-artifact@018cc2cf5baa6db3ef3c5f8a56943fffe632ef53 # v6
      with:
        name: build-jar
        path: build/libs/
    - name: Set DEBUG_ARGS env var
      env:
        RUNNER_DEBUG: ${{ runner.debug }}
      shell: bash
      run: |-
        if [[ "$RUNNER_DEBUG" -eq 1 ]]; then
          echo "DEBUG_ARGS=--info --stacktrace" >> $GITHUB_ENV
        fi
    - run: ./gradlew "$DEBUG_ARGS" verifyDistribution
    - name: Upload Test Result XML
      if: '!cancelled()'
      uses: actions/upload-artifact@330a01c490aca151604b8cf639adc76d48f6c5d4 # v5
      with:
        name: test-results-xml-test-macos
        path: build/test-results/**/*.xml
  test-windows:
    needs:
    - build-release
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6
      with:
        persist-credentials: false
    - uses: actions/setup-java@1bcf9fb12cf4aa7d266a90ae39939e61372fe520 # v5
      with:
        java-version: '25'
        distribution: temurin
    - uses: actions/download-artifact@018cc2cf5baa6db3ef3c5f8a56943fffe632ef53 # v6
      with:
        name: build-jar
        path: build/libs/
    - name: Set DEBUG_ARGS env var
      env:
        RUNNER_DEBUG: ${{ runner.debug }}
      shell: bash
      run: |-
        if [[ "$RUNNER_DEBUG" -eq 1 ]]; then
          echo "DEBUG_ARGS=--info --stacktrace" >> $GITHUB_ENV
        fi
    - run: ./gradlew "$DEBUG_ARGS" verifyDistribution
    - name: Upload Test Result XML
      if: '!cancelled()'
      uses: actions/upload-artifact@330a01c490aca151604b8cf639adc76d48f6c5d4 # v5
      with:
        name: test-results-xml-test-windows
        path: build/test-results/**/*.xml
  native-macos-aarch64:
    if: github.repository_owner == 'apple'
    runs-on:
    - self-hosted
    - macos
    steps:
    - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6
      with:
        persist-credentials: false
        fetch-depth: 0
    - uses: actions/setup-java@1bcf9fb12cf4aa7d266a90ae39939e61372fe520 # v5
      with:
        java-version: '25'
        distribution: temurin
        cache: gradle
    - name: Set DEBUG_ARGS env var
      env:
        RUNNER_DEBUG: ${{ runner.debug }}
      shell: bash
      run: |-
        if [[ "$RUNNER_DEBUG" -eq 1 ]]; then
          echo "DEBUG_ARGS=--info --stacktrace" >> $GITHUB_ENV
        fi
    - name: Build native executable
      shell: bash
      run: ./gradlew $DEBUG_ARGS buildNative
    - name: Upload native executable
      if: '!cancelled()'
      uses: actions/upload-artifact@330a01c490aca151604b8cf639adc76d48f6c5d4 # v5
      with:
        name: build-native-macos-aarch64
        path: build*/executable/*
    - name: Upload Test Result XML
      if: '!cancelled()'
      uses: actions/upload-artifact@330a01c490aca151604b8cf639adc76d48f6c5d4 # v5
      with:
        name: test-results-xml-native-macos-aarch64
        path: build/test-results/**/*.xml
  native-linux-amd64:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6
      with:
        persist-credentials: false
        fetch-depth: 0
    - uses: actions/setup-java@1bcf9fb12cf4aa7d266a90ae39939e61372fe520 # v5
      with:
        java-version: '25'
        distribution: temurin
        cache: gradle
    - name: Install deps
      run: dnf install -y git binutils gcc glibc-devel zlib-devel libstdc++-static glibc-langpack-en xz
    - name: Set DEBUG_ARGS env var
      env:
        RUNNER_DEBUG: ${{ runner.debug }}
      shell: bash
      run: |-
        if [[ "$RUNNER_DEBUG" -eq 1 ]]; then
          echo "DEBUG_ARGS=--info --stacktrace" >> $GITHUB_ENV
        fi
    - name: Build native executable
      shell: bash
      run: ./gradlew $DEBUG_ARGS buildNative
    - name: Upload native executable
      if: '!cancelled()'
      uses: actions/upload-artifact@330a01c490aca151604b8cf639adc76d48f6c5d4 # v5
      with:
        name: build-native-linux-amd64
        path: build*/executable/*
    - name: Upload Test Result XML
      if: '!cancelled()'
      uses: actions/upload-artifact@330a01c490aca151604b8cf639adc76d48f6c5d4 # v5
      with:
        name: test-results-xml-native-linux-amd64
        path: build/test-results/**/*.xml
    container:
      image: redhat/ubi8:8.10
  native-linux-aarch64:
    runs-on: ubuntu-24.04-arm
    steps:
    - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6
      with:
        persist-credentials: false
        fetch-depth: 0
    - uses: actions/setup-java@1bcf9fb12cf4aa7d266a90ae39939e61372fe520 # v5
      with:
        java-version: '25'
        distribution: temurin
        cache: gradle
    - name: Install deps
      run: dnf install -y git binutils gcc glibc-devel zlib-devel libstdc++-static glibc-langpack-en xz
    - name: Set DEBUG_ARGS env var
      env:
        RUNNER_DEBUG: ${{ runner.debug }}
      shell: bash
      run: |-
        if [[ "$RUNNER_DEBUG" -eq 1 ]]; then
          echo "DEBUG_ARGS=--info --stacktrace" >> $GITHUB_ENV
        fi
    - name: Build native executable
      shell: bash
      run: ./gradlew $DEBUG_ARGS buildNative
    - name: Upload native executable
      if: '!cancelled()'
      uses: actions/upload-artifact@330a01c490aca151604b8cf639adc76d48f6c5d4 # v5
      with:
        name: build-native-linux-aarch64
        path: build*/executable/*
    - name: Upload Test Result XML
      if: '!cancelled()'
      uses: actions/upload-artifact@330a01c490aca151604b8cf639adc76d48f6c5d4 # v5
      with:
        name: test-results-xml-native-linux-aarch64
        path: build/test-results/**/*.xml
    container:
      image: redhat/ubi8:8.10
  native-windows-amd64:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6
      with:
        persist-credentials: false
        fetch-depth: 0
    - uses: actions/setup-java@1bcf9fb12cf4aa7d266a90ae39939e61372fe520 # v5
      with:
        java-version: '25'
        distribution: temurin
        cache: gradle
    - name: Set DEBUG_ARGS env var
      env:
        RUNNER_DEBUG: ${{ runner.debug }}
      shell: bash
      run: |-
        if [[ "$RUNNER_DEBUG" -eq 1 ]]; then
          echo "DEBUG_ARGS=--info --stacktrace" >> $GITHUB_ENV
        fi
    - name: Build native executable
      shell: bash
      run: ./gradlew $DEBUG_ARGS buildNative
    - name: Upload native executable
      if: '!cancelled()'
      uses: actions/upload-artifact@330a01c490aca151604b8cf639adc76d48f6c5d4 # v5
      with:
        name: build-native-windows-amd64
        path: build*/executable/*
    - name: Upload Test Result XML
      if: '!cancelled()'
      uses: actions/upload-artifact@330a01c490aca151604b8cf639adc76d48f6c5d4 # v5
      with:
        name: test-results-xml-native-windows-amd64
        path: build/test-results/**/*.xml
  deploy-github-release:
    needs:
    - test-macos
    - test-windows
    - native-macos-aarch64
    - native-linux-amd64
    - native-linux-aarch64
    - native-windows-amd64
    permissions:
      contents: write
    runs-on: ubuntu-latest
    steps:
    - uses: actions/download-artifact@018cc2cf5baa6db3ef3c5f8a56943fffe632ef53 # v6
      with:
        pattern: build-**
        merge-multiple: true
    - name: Publish release to GitHub
      env:
        GH_TOKEN: ${{ github.token }}
        GH_REPO: ${{ github.repository }}
        VERSION: ${{ github.ref_name }}
      run: |-
        # exclude build_artifacts.txt from publish
        rm -f */build/executable/*.build_artifacts.txt
        find */build/executable/* -type d | xargs rm -rf
        gh release create "${VERSION}" \
          --title "${VERSION}" \
          --verify-tag \
          --notes "Release notes: https://pkl-lang.org/lsp/current/CHANGELOG.html#release-${VERSION}" \
          build/libs/* build/executable/*
  deploy-maven-release:
    needs:
    - deploy-github-release
    runs-on: ubuntu-latest
    environment: maven-release
    steps:
    - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6
      with:
        persist-credentials: false
    - uses: actions/setup-java@1bcf9fb12cf4aa7d266a90ae39939e61372fe520 # v5
      with:
        java-version: '25'
        distribution: temurin
    - uses: actions/download-artifact@018cc2cf5baa6db3ef3c5f8a56943fffe632ef53 # v6
      with:
        name: pkl-lsp-build-artifacts
        path: build/libs/
    - name: Publish to Maven Central
      env:
        ORG_GRADLE_PROJECT_signingKeyId: ${{ secrets.ORG_GRADLE_PROJECT_SIGNINGKEYID }}
        ORG_GRADLE_PROJECT_signingKey: ${{ secrets.ORG_GRADLE_PROJECT_SIGNINGKEY }}
        ORG_GRADLE_PROJECT_signingPassword: ${{ secrets.ORG_GRADLE_PROJECT_SIGNINGPASSWORD }}
        ORG_GRADLE_PROJECT_sonatypePassword: ${{ secrets.ORG_GRADLE_PROJECT_SONATYPEPASSWORD }}
        ORG_GRADLE_PROJECT_sonatypeUsername: ${{ secrets.ORG_GRADLE_PROJECT_SONATYPEUSERNAME }}
      run: ./gradlew -DreleaseBuild=true publishToSonatype closeAndReleaseSonatypeStagingRepository
  publish-test-results:
    if: '!cancelled()'
    needs:
    - build-release
    - test-macos
    - test-windows
    - native-macos-aarch64
    - native-linux-amd64
    - native-linux-aarch64
    - native-windows-amd64
    permissions:
      checks: write
    runs-on: ubuntu-latest
    steps:
    - uses: actions/download-artifact@018cc2cf5baa6db3ef3c5f8a56943fffe632ef53 # v6
      with:
        pattern: test-results-xml-*
    - name: Publish test results
      if: '!cancelled()'
      uses: EnricoMi/publish-unit-test-result-action@d0a4676d0e0b938bc201470d88276b7c74c712b3 # v2
      with:
        comment_mode: 'off'
        files: test-results-xml-*/**/*.xml
